import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as stories from './Spinner.stories'

<Meta of={stories} />

# Spinner

> Spinner brukes for å indikere at innhold eller en handling er i ferd med å laste, og at brukeren må vente før de kan fortsette.

> Se full dokumentasjon for komponenten på [designsystemet.no](https://designsystemet.no/no/components/docs/spinner/overview)

## Kode

- Bruk `<Spinner>` komponenten i React

<Canvas of={stories.Default} />

## Størrelse

- Bruk `data-size` for å endre størrelse

<Canvas of={stories.Size} />

## Med tekst

<Canvas of={stories.Text} />
