import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as stories from './Select.stories'

<Meta of={stories} />

# Select

> Select lar brukeren velge ett alternativ fra en liste.

> Se full dokumentasjon for komponenten på [designsystemet.no](https://designsystemet.no/no/components/docs/select/overview)

## Kode

- Bruk `<Select>` komponenten i React

<Canvas of={stories.Default} />

## Gruppering

- Gruppering brukes for å organisere alternativer i kategorier

<Canvas of={stories.Group} />

## ReadOnly

- Bruk `aria-readonly` på Select når du vil vise en forhåndsvalgt verdi i select, men ikke tillate at brukeren endrer valget.

<Canvas of={stories.ReadOnly} />
