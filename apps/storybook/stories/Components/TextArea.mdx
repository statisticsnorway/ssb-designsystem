import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as stories from './TextArea.stories'

<Meta of={stories} />

# Textarea

> Textarea brukes når brukeren skal kunne skrive inn tekst som går over flere linjer.

> Se full dokumentasjon for komponenten på [designsystemet.no](https://designsystemet.no/no/components/docs/textarea/overview)

## Kode

- Bruk `<Textarea>` komponenten i React

<Canvas of={stories.Default} />

## Med høyde og bredde

- Bruk `rows`-attributtet for å sette antall synlige rader i tekstområdet.
- Bruk `cols`-attributtet for å sette antall tegn i bredden.

<Canvas of={stories.WidthHeight} />
