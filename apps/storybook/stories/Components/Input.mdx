import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as stories from './Input.stories'

<Meta of={stories} />

# Input

> Input lar brukeren skrive inn én linje med tekst.

> Se full dokumentasjon for komponenten på [designsystemet.no](https://designsystemet.no/en/components/docs/input/overview)

## Kode

- Bruk `<Input>` for enkle tekstfelt i skjemaer

  <Canvas of={stories.Default} />

## Varianter

- Se ulike tilstander og oppsett

  <Canvas of={stories.Variants} />