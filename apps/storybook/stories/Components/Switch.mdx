import { Meta, Canvas } from '@storybook/addon-docs/blocks'
import * as stories from './Switch.stories'

<Meta of={stories} />

# Switch

> Switch lar brukerne velge mellom to alternativer ved å slå noe av eller på.

> Se full dokumentasjon for komponenten på [designsystemet.no](https://designsystemet.no/no/components/docs/switch/overview)

## Kode

- Bruk `<Switch>` komponenten i React

<Canvas of={stories.Default} />

## Gruppering

- Bruk `<Fieldset>` for å gruppere flere switch-komponenter sammen.

<Canvas of={stories.Group} />

## Outline

- Bruk `variant='outline'` for å legge på ekstra padding og en ramme rundt valget slik at det blir en større klikkflate.

<Canvas of={stories.Outline} />

## Høyrejustert

- Bruk `position='end'` for å legge ledeteksten først

  <Canvas of={stories.RightAligned} />

## Med beskrivelse

- Bruk `description'` for å legge til beskrivelse

  <Canvas of={stories.WithDescription} />
